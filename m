#!/bin/bash

pushd util        && m && popd

pushd filetime    && m && popd
pushd firstpar    && m && popd
pushd gitdate     && m && popd
pushd gitfind     && m && popd
pushd gitnav      && m && popd

