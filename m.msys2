#!/bin/bash

( cd util        && m.msys2 )

( cd filetime    && m.msys2 )
( cd firstpar    && m.msys2 )
( cd gitdate     && m.msys2 )
( cd gitfind     && m.msys2 )
( cd gitnav      && m.msys2 )

install
