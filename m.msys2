#!/bin/bash

pushd util        && m.msys2 && popd

pushd filetime    && m.msys2 && popd
pushd firstpar    && m.msys2 && popd
pushd gitdate     && m.msys2 && popd
pushd gitfind     && m.msys2 && popd
pushd gitnav      && m.msys2 && popd

